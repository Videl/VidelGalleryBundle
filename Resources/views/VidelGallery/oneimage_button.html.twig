{% extends 'VidelGalleryBundle::base.html.twig' %}

{% block body %}
<button>
<h1>{{ image.name }}, {{ image.id }}</h1>
<img src="http://localhost/www/symfony/web/{{ image.getWebPath }}" alt="{{ image.name }}" />
</button>
{% endblock %}