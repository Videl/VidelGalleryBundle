{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Gallery creation</h1>

    <form action="{{ path('gallery_crud_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('gallery_crud') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
